#include <stdio.h>

    void int_data_type(){
       long int a= 731470;
      printf("Size of int : %zu bytes\n", sizeof(a));
      printf("Value of a %ld\n", a);
    }

    int main(){
        int_data_type();
        return 0;
             }

 //printf("Size of int: %zu bytes\n", sizeof(a));
      // printf("value of a %lld\n", a);
